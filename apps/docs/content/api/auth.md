---
title: Auth & Account API
description: Registration, sign-in, email verification, password reset, OAuth, and the signed-in account.
---

These endpoints back the Livqeno dashboard and the CLI's `raven login`.
Your own application's users never touch them — they authenticate against
your app, and your backend mints them a [token](/authentication/tokens).
{/* generated:endpoints — do not edit by hand */}

{/* Generated by scripts/generate-api-reference.mjs from apps/api/src.
    Edit the controllers or the generator, never this block. */}

## Endpoints

| Method | Path | Credential |
|---|---|---|
| POST | [`/v1/auth/login`](#post-v1authlogin) | None |
| POST | [`/v1/auth/logout`](#post-v1authlogout) | Dashboard session (JWT) |
| POST | [`/v1/auth/oauth/{provider}/exchange`](#post-v1authoauthproviderexchange) | None |
| POST | [`/v1/auth/oauth/{provider}/start`](#post-v1authoauthproviderstart) | None |
| GET | [`/v1/auth/oauth/providers`](#get-v1authoauthproviders) | None |
| POST | [`/v1/auth/password-reset`](#post-v1authpasswordreset) | None |
| POST | [`/v1/auth/password-reset/confirm`](#post-v1authpasswordresetconfirm) | None |
| POST | [`/v1/auth/register`](#post-v1authregister) | None |
| POST | [`/v1/auth/verify-email`](#post-v1authverifyemail) | None |
| POST | [`/v1/auth/verify-email/resend`](#post-v1authverifyemailresend) | Dashboard session (JWT) |
| GET | [`/v1/onboarding`](#get-v1onboarding) | None |
| PATCH | [`/v1/onboarding`](#patch-v1onboarding) | None |
| POST | [`/v1/onboarding/complete`](#post-v1onboardingcomplete) | None |
| GET | [`/v1/users/me`](#get-v1usersme) | None |
| PATCH | [`/v1/users/me`](#patch-v1usersme) | None |

### POST `/v1/auth/login`

Exchange email/password for a session JWT

**Credential** None · **Rate limit** 10 per window ([details](/production/rate-limits))

| Parameter | In | Type | Required | Notes |
|---|---|---|---|---|
| `email` | body | `string` | Yes |  |
| `password` | body | `string` | Yes |  |

### POST `/v1/auth/logout`

Invalidate the current session JWT

**Credential** Dashboard session (JWT)

_No parameters._

### POST `/v1/auth/oauth/{provider}/exchange`

Complete an OAuth sign-in

**Credential** None · **Rate limit** 10 per window ([details](/production/rate-limits))

| Parameter | In | Type | Required | Notes |
|---|---|---|---|---|
| `provider` | path | `string` | Yes | |
| `code` | body | `string` | Yes | 0–512 chars |
| `state` | body | `string` | Yes | 0–128 chars |

### POST `/v1/auth/oauth/{provider}/start`

Begin an OAuth sign-in

**Credential** None · **Rate limit** 20 per window ([details](/production/rate-limits))

| Parameter | In | Type | Required | Notes |
|---|---|---|---|---|
| `provider` | path | `string` | Yes | |

### GET `/v1/auth/oauth/providers`

Which OAuth providers this deployment has configured

**Credential** None

_No parameters._

### POST `/v1/auth/password-reset`

Request a password-reset link

**Credential** None · **Rate limit** 5 per window ([details](/production/rate-limits))

| Parameter | In | Type | Required | Notes |
|---|---|---|---|---|
| `email` | body | `string` | Yes |  |

### POST `/v1/auth/password-reset/confirm`

Set a new password using the token from a reset email

**Credential** None · **Rate limit** 5 per window ([details](/production/rate-limits))

| Parameter | In | Type | Required | Notes |
|---|---|---|---|---|
| `token` | body | `string` | Yes | 20–200 chars — The token from the link in the password-reset email. |
| `password` | body | `string` | Yes | 8–72 chars |

### POST `/v1/auth/register`

Create a developer account

**Credential** None · **Rate limit** 5 per window ([details](/production/rate-limits))

| Parameter | In | Type | Required | Notes |
|---|---|---|---|---|
| `email` | body | `string` | Yes |  |
| `password` | body | `string` | Yes | 8–72 chars |
| `name` | body | `string` | No | 0–120 chars |

### POST `/v1/auth/verify-email`

Confirm an email address using the token from a verification email

**Credential** None · **Rate limit** 10 per window ([details](/production/rate-limits))

| Parameter | In | Type | Required | Notes |
|---|---|---|---|---|
| `token` | body | `string` | Yes | 20–200 chars — The token from the link in the verification email. |

### POST `/v1/auth/verify-email/resend`

Send another verification link to the signed-in account

**Credential** Dashboard session (JWT) · **Rate limit** 3 per window ([details](/production/rate-limits))

_No parameters._

### GET `/v1/onboarding`

The signed-in user's onboarding state

**Credential** None

_No parameters._

### PATCH `/v1/onboarding`

Save onboarding progress

**Credential** None

| Parameter | In | Type | Required | Notes |
|---|---|---|---|---|
| `step` | body | `number` | No |  |
| `useCases` | body | `string[]` | No | 0–64 chars, array |
| `experienceLevel` | body | `string` | No | `getting-started` · `some-experience` · `experienced` · `production` |
| `stack` | body | `string[]` | No | 0–64 chars, array |
| `createdFirstProject` | body | `boolean` | No |  |

### POST `/v1/onboarding/complete`

Mark onboarding finished

**Credential** None

_No parameters._

### GET `/v1/users/me`

The signed-in account

**Credential** None

_No parameters._

### PATCH `/v1/users/me`

Update the signed-in account profile

**Credential** None

| Parameter | In | Type | Required | Notes |
|---|---|---|---|---|
| `name` | body | `string` | No | 0–120 chars |

{/* /generated:endpoints */}
## Next steps

- [Conventions](/api/conventions) — base path, error envelope, request ids, pagination.
- [Errors](/reference/errors) — every code this API can return.
- [All endpoints](/api/all-endpoints) — the whole surface on one page.
